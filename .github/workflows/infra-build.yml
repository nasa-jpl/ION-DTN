name: Build and Push Runner Images to Registry

permissions:
  contents: read

on:
  push:
    branches: ["integration"]
    paths:
      - "images/**"

  workflow_dispatch:
    inputs:
      runner_u22:
        description: "Build Ubuntu 22.04 (arc-runner-set-u22)"
        required: false
        type: boolean
        default: true
      runner_u24:
        description: "Build Ubuntu 24.04 (arc-runner-set-u24)"
        required: false
        type: boolean
        default: true
      runner_ol8:
        description: "Build Oracle Linux 8 (arc-runner-set-ol8)"
        required: false
        type: boolean
        default: true
      runner_ol9:
        description: "Build Oracle Linux 9 (arc-runner-set-ol9)"
        required: false
        type: boolean
        default: true
      runner_rhel8:
        description: "Build RHEL 8 (arc-runner-set-rhel8)"
        required: false
        type: boolean
        default: true
      runner_rhel9:
        description: "Build RHEL 9 (arc-runner-set-rhel9)"
        required: false
        type: boolean
        default: true
  workflow_call:
    inputs:
      runner_u22:
        description: "Build Ubuntu 22.04 (arc-runner-set-u22)"
        required: false
        type: boolean
        default: true
      runner_u24:
        description: "Build Ubuntu 24.04 (arc-runner-set-u24)"
        required: false
        type: boolean
        default: true

      runner_ol8:
        description: "Build Oracle Linux 8 (arc-runner-set-ol8)"
        required: false
        type: boolean
        default: true
      runner_ol9:
        description: "Build Oracle Linux 9 (arc-runner-set-ol9)"
        required: false
        type: boolean
        default: true
      runner_rhel8:
        description: "Build RHEL 8 (arc-runner-set-rhel8)"
        required: false
        type: boolean
        default: true
      runner_rhel9:
        description: "Build RHEL 9 (arc-runner-set-rhel9)"
        required: false
        type: boolean
        default: true

env:
  REGISTRY_ADDRESS: ${{ secrets.REGISTRY_ADDRESS }}
  REGISTRY_NAMESPACE: ${{ secrets.REGISTRY_NAMESPACE }}
  REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
  PIP_INDEX: ${{ secrets.PIP_INDEX }}

jobs:
  setup:
    runs-on: arc-runner-set-u22
    outputs:
      matrix: ${{ steps.build-matrix.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Build OS Matrix
        id: build-matrix
        run: |
          MATRIX=$(bash .github/scripts/build-runner-matrix.sh \
            --ubuntu_20=${{ github.event_name == 'push' || inputs.runner_u22 == true }} \
            --ubuntu_22=${{ github.event_name == 'push' || inputs.runner_u24 == true }} \
            --oracle_linux_8=${{ github.event_name == 'push' || inputs.runner_ol8 == true }} \
            --oracle_linux_9=${{ github.event_name == 'push' || inputs.runner_ol9 == true }} \
            --rhel_8=${{ github.event_name == 'push' || inputs.runner_rhel8 == true }} \
            --rhel_9=${{ github.event_name == 'push' || inputs.runner_rhel9 == true }})

          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT

  build-and-push:
    name: Build and Push ${{ matrix.runner }}
    needs: setup
    runs-on: arc-runner-set-${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        runner: ${{ fromJSON(needs.setup.outputs.matrix) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Login to container registry
        run: |
          echo "${{ env.REGISTRY_PASSWORD }}" | sudo buildah login ${{ env.REGISTRY_ADDRESS }} --username "${{ env.REGISTRY_NAMESPACE }}" --password-stdin

      - name: Set Image Variables
        id: vars
        run: |
          case "${{ matrix.runner }}" in
            u20)   echo "os_image=ubuntu-22" >> $GITHUB_OUTPUT; echo "target=buildah-build" >> $GITHUB_OUTPUT ;;
            u22)   echo "os_image=ubuntu-24" >> $GITHUB_OUTPUT; echo "target=buildah-build" >> $GITHUB_OUTPUT ;;
            ol8)   echo "os_image=oraclelinux-8" >> $GITHUB_OUTPUT; echo "target=buildah-build" >> $GITHUB_OUTPUT ;;
            ol9)   echo "os_image=oraclelinux-9" >> $GITHUB_OUTPUT; echo "target=buildah-build" >> $GITHUB_OUTPUT ;;
            rhel8) echo "os_image=rhel-8" >> $GITHUB_OUTPUT; echo "target=buildah-build-rhel" >> $GITHUB_OUTPUT ;;
            rhel9) echo "os_image=rhel-9" >> $GITHUB_OUTPUT; echo "target=buildah-build-rhel" >> $GITHUB_OUTPUT ;;
          esac

      - name: Build the container image
        run: |
          cd images && \
          make ${{ steps.vars.outputs.target }} \
            PIP_INDEX=${{ env.PIP_INDEX }} \
            OS_IMAGE=${{ steps.vars.outputs.os_image }} \
            DEFAULT_RUNNER_NAME=${{ env.REGISTRY_ADDRESS }}/${{ env.REGISTRY_NAMESPACE }}/ion-runner-images

      - name: Push the container image
        run: |
          cd images && \
          make buildah-push \
            OS_IMAGE=${{ steps.vars.outputs.os_image }} \
            DEFAULT_RUNNER_NAME=${{ env.REGISTRY_ADDRESS }}/${{ env.REGISTRY_NAMESPACE }}/ion-runner-images

      - name: Buildah Logout
        if: ${{ always() }}
        run: sudo buildah logout ${{ env.REGISTRY_ADDRESS }}
